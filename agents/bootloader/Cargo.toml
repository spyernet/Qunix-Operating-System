[package]
name = "qunix-bootloader"
version = "0.2.0"
edition = "2021"

[dependencies]
uefi          = "0.18"
uefi-services = "0.15"

[[bin]]
name = "bootloader"
path = "src/main.rs"

[profile.release]
opt-level     = "s"
panic         = "abort"
lto           = true
codegen-units = 1
