[package]
name = "basename"
version = "1.0.0"
edition = "2021"

[dependencies]
libsys = { path = "../libsys" }

[[bin]]
name = "basename"
path = "src/main.rs"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
codegen-units = 1
