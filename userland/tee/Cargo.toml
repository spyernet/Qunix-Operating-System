[package]
name = "tee"
version = "0.1.0"
edition = "2021"

[dependencies]
libsys = { path = "../libsys" }

[[bin]]
name = "tee"
path = "src/main.rs"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
codegen-units = 1
